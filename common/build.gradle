architectury {
    common()
}

loom {
    accessWidener =  file("src/main/resources/etched.accesswidener")
}

dependencies {
    modImplementation "net.fabricmc:fabric-loader:${rootProject.fabric_loader_version}"
}